# Kit.com API Configuration
# Create an API key (v3 or v4) at: https://app.kit.com/account_settings/advanced_settings
# Note: Both v3 and v4 API keys work with the v4 endpoint
KIT_API_KEY=your_api_key_here

# Tag name to apply to waitlist subscribers (optional, defaults to "instareply-waitlist")
# This helps you identify subscribers who came from the Instareply waitlist
KIT_TAG_NAME=instareply-waitlist

# OpenAI API Configuration (Optional)
# If not provided, the app will use demo/mock responses for testing
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
